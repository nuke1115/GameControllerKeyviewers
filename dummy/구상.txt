//이건 사볼콘 키뷰어
//라이브러리 : sfml
//언어 : c++

루틴: <- Application 이 담당
이벤트 감지/확인 <- EventDetector 가 담당
이벤트 처리(종료,이미지 바꾸기) <- EventHandler 가 담당
화면 업데이트 <- WindowManager 가 담당

처음에 ini파일을 불러와서 각 키와 노브의 번호가 몇인지 JoystickCodes에 지정해준다


ini파일에 각 버튼이 몇번 번호에 해당하는지, 노브가 어떤 축에 해당하는지 적어두면, 프로그램이 시작할때 읽어와서 JoystickCodes에 할당해준다
JoystickCodes에 할당된 값들은 처음 초기화과정 이후에는 값을 읽어오기만 한다(==enum)

AppEvent구조체에 각 값들을 업데이트해준다



인라인 함수 뜯어고치기
vs 띄어쓰기, 띄어쓰기같은거 고치기
vs 탭에서 스페이스4개로 바꾸기



JoystickIndex static변수를 namespace에만 넣어뒀을때 값 대입이 불가한 문제
=>Index 구조체에 넣음으로써 해결   
///////////
프로그램 내에서 키 세팅 가능하도록 수정
프로그램 내에서 키 세팅 가능하도록 수정

ctrl+s키를 누르면 키 세팅 모드로 진입
화면에 어떤 키를 세팅중인지를 띄우고, 원하는 버튼을 눌렀다 땐 뒤(노브의 경우에는 원하는 노브를 움직인다), 엔터를 누르면 다음으로 넘어가는 형식


세팅해야될 키를 깜빡거리게 해보자
이건 멀티스레딩으로 처리하면 될거같다?
일단은 싱글스레드로 해서, count변수가 특정 값을 넘을때마다 반전시키도록 해뒀다.
멀티스레딩으로 하려면 코드의 구조를 조금 많이 뜯어고쳐야된다
이건 나중에 된다면 이걸로 변환해두자

drawableObjectManager 매개변수로 받는거 싹다 뜯어고치기